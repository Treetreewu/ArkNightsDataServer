from models.Effect import Type, ValueTarget, Target

effects = {
    "header": [Type, ValueTarget, Target, "Count", "Duration", "Range"],
    "": []
}

a = ["network", "subnet", "port", "router", "floatingip", "security_group",
     "security_group_rule", "firewall"]
iterable = 1
